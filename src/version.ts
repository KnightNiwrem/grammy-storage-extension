/**
 * The package version recorded in every envelope written by this adapter.
 * Must match `version` in `deno.json` (enforced by the test suite).
 */
export const PACKAGE_VERSION = "0.1.0" as const;
